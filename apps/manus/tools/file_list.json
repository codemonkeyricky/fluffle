{
  "name": "file_list",
  "description": "List files in a directory",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Relative path to the directory to list (must be relative, not absolute; default: current directory)",
        "default": "."
      }
    },
    "required": []
  },
  "execution": {
    "type": "bash",
    "command_template": "ls -la {{path}}"
  },
  "secure_parameters": ["path"]
}
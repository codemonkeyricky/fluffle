{
  "name": "git_diff",
  "description": "Show git diff of the current repository",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Optional path to show diff for (default: all changes)",
        "default": ""
      }
    },
    "required": []
  },
  "execution": {
    "type": "bash",
    "command_template": "path='{{path}}'; git diff ${path:+-- $path}"
  },
  "secure_parameters": []
}
